<Window x:Class="MacroStudio.Controls.CorePickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择核心"
        Width="980"
        Height="520"
        MinWidth="520"
        MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PageBackground}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="HelpText"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryText}"
                   Margin="0,0,0,10" />
        <Border Grid.Row="1" Style="{StaticResource PanelBorderStyle}" Padding="6">
            <ListView x:Name="CoreList"
                      SelectionMode="Extended"
                      Background="{DynamicResource PanelRaised}"
                      Foreground="{DynamicResource PrimaryText}"
                      BorderThickness="0">
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource PanelBackground}" />
                        <Setter Property="Foreground" Value="{DynamicResource SecondaryText}" />
                        <Setter Property="Padding" Value="8,6" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="GridViewColumnHeader">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{DynamicResource PanelBorder}"
                                            BorderThickness="0,0,1,1"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"
                                                          RecognizesAccessKey="True"
                                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="Padding" Value="4,3" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Row"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            TextElement.Foreground="{TemplateBinding Foreground}">
                                        <GridViewRowPresenter Columns="{TemplateBinding GridView.ColumnCollection}"
                                                              Content="{TemplateBinding Content}"
                                                              VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Row" Property="Background" Value="{DynamicResource Hover}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Row" Property="Background" Value="{DynamicResource Selection}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="70">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="90" DisplayMemberBinding="{Binding ProcessorText}" />
                        <GridViewColumn Width="90" DisplayMemberBinding="{Binding PhysicalText}" />
                        <GridViewColumn Width="90" DisplayMemberBinding="{Binding KindText}" />
                        <GridViewColumn Width="180" DisplayMemberBinding="{Binding SiblingText}" />
                        <GridViewColumn Width="150" DisplayMemberBinding="{Binding ResultText}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
        <TextBlock x:Name="ResultText"
                   Grid.Row="2"
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"
                   Foreground="{DynamicResource SecondaryText}" />
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="SelectAllButton"
                        MinWidth="72"
                        Content="全选"
                        Click="SelectAllButton_Click" />
                <Button x:Name="SelectNoneButton"
                        MinWidth="82"
                        Margin="8,0,0,0"
                        Content="全不选"
                        Click="SelectNoneButton_Click" />
                <Button x:Name="TestButton"
                        MinWidth="82"
                        Margin="8,0,0,0"
                        Content="测试"
                        Click="TestButton_Click" />
                <TextBlock x:Name="AutoSelectCountLabel"
                           Margin="16,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource SecondaryText}"
                           Text="数量" />
                <TextBox x:Name="AutoSelectCountBox"
                         Width="56"
                         Height="34"
                         Margin="8,0,0,0"
                         Padding="0"
                         VerticalAlignment="Center"
                         HorizontalContentAlignment="Center"
                         VerticalContentAlignment="Center"
                         TextAlignment="Center"
                         Text="2">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="11"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="6,0"
                                              Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Disabled" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource FocusRing}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
                <Button x:Name="AutoSelectButton"
                        MinWidth="96"
                        Margin="8,0,0,0"
                        Content="自动选择"
                        Click="AutoSelectButton_Click" />
                <Button x:Name="SortByResultButton"
                        MinWidth="110"
                        Margin="8,0,0,0"
                        Content="按结果排序"
                        Click="SortByResultButton_Click" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" MinWidth="82" IsCancel="True" Content="取消" />
                <Button x:Name="ConfirmButton"
                        MinWidth="82"
                        Margin="8,0,0,0"
                        Style="{StaticResource PrimaryButton}"
                        IsDefault="True"
                        Content="确定"
                        Click="ConfirmButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
