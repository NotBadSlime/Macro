<Window x:Class="MacroStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MacroHID Studio" Height="760" Width="1180"
        MinHeight="620" MinWidth="980"
        Background="#F6F7F9">
    <Window.Resources>
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="MinWidth" Value="92" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="12,0" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="BorderBrush" Value="#D9DEE7" />
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#1F2937" Padding="16,12">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="MacroHID" Foreground="White" FontSize="19" FontWeight="SemiBold" Margin="0,0,18,0" />
                    <Button x:Name="OpenButton" Style="{StaticResource ToolbarButton}" Content="Open" Click="OpenMacro_Click" />
                    <Button x:Name="SaveButton" Style="{StaticResource ToolbarButton}" Content="Save" Click="SaveMacro_Click" />
                    <Button x:Name="ValidateButton" Style="{StaticResource ToolbarButton}" Content="Validate" Click="ValidateMacro_Click" />
                    <Button x:Name="ProbeButton" Style="{StaticResource ToolbarButton}" Content="Probe" Click="Probe_Click" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="LanguageLabelText" Foreground="#D1D5DB" VerticalAlignment="Center" Margin="0,0,8,0" Text="Language" />
                    <ComboBox x:Name="LanguageComboBox"
                              Width="150"
                              Height="30"
                              Margin="0,0,18,0"
                              SelectionChanged="LanguageComboBox_SelectionChanged" />
                    <TextBlock x:Name="StatusText" Foreground="#D1D5DB" VerticalAlignment="Center" Text="Driver offline" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="460" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="1.35*" MinWidth="340" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="MCRX" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBox x:Name="MacroEditor"
                         Grid.Row="1"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         FontFamily="Consolas"
                         FontSize="14"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap"
                         Padding="12"
                         BorderBrush="#C7CEDA"
                         Background="White" />
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <GroupBox x:Name="MacroGroupBox" Header="Macro">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="NameLabelText" Text="Name" Foreground="#4B5563" />
                            <TextBlock x:Name="MacroNameText" FontSize="20" FontWeight="SemiBold" Text="-" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock x:Name="ScheduledStepsLabelText" Text="Scheduled Steps" Foreground="#4B5563" />
                            <TextBlock x:Name="StepCountText" FontSize="20" FontWeight="SemiBold" Text="0" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox x:Name="PlaybackGroupBox" Grid.Row="1" Header="Playback">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="96" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.ColumnSpan="3">
                            <TextBlock x:Name="TriggerLabelText" Text="Trigger" Foreground="#4B5563" />
                            <DockPanel Margin="0,3,0,8">
                                <Button x:Name="CaptureTriggerButton" DockPanel.Dock="Right" Width="78" Content="Capture" Click="CaptureTrigger_Click" />
                                <TextBox x:Name="TriggerTextBox" Height="28" Margin="0,0,8,0" VerticalContentAlignment="Center" />
                            </DockPanel>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock x:Name="ModeLabelText" Text="Mode" Foreground="#4B5563" />
                            <ComboBox x:Name="PlaybackModeBox" Height="28" SelectionChanged="PlaybackModeBox_SelectionChanged">
                                <ComboBoxItem Tag="toggleLoop" Content="toggleLoop" />
                                <ComboBoxItem Tag="holdLoop" Content="holdLoop" />
                                <ComboBoxItem Tag="fixedCount" Content="fixedCount" IsSelected="True" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <TextBlock x:Name="PlaybackCountLabelText" Text="Count" Foreground="#4B5563" />
                            <TextBox x:Name="PlaybackCountTextBox" Height="28" Text="1" VerticalContentAlignment="Center" />
                        </StackPanel>

                        <WrapPanel Grid.Row="2" Grid.ColumnSpan="3" Margin="0,10,0,0">
                            <Button x:Name="StartListeningButton" MinWidth="104" Height="30" Margin="0,0,8,8" Content="Start Listening" Click="StartListening_Click" />
                            <Button x:Name="StopListeningButton" MinWidth="104" Height="30" Margin="0,0,8,8" Content="Stop Listening" Click="StopListening_Click" />
                            <Button x:Name="RunNowButton" MinWidth="78" Height="30" Margin="0,0,8,8" Content="Run Now" Click="RunNow_Click" />
                            <Button x:Name="StopPlaybackButton" MinWidth="64" Height="30" Margin="0,0,0,8" Content="Stop" Click="StopPlayback_Click" />
                        </WrapPanel>

                        <StackPanel Grid.Row="3" Grid.ColumnSpan="3">
                            <TextBlock x:Name="PlaybackStatusText" Text="Playback: Idle" />
                            <TextBlock x:Name="PlaybackResultText" Text="Last result: none" Margin="0,4,0,0" Foreground="#4B5563" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox x:Name="SequenceGroupBox" Grid.Row="2" Header="Sequence">
                    <ListBox x:Name="StepList" FontFamily="Consolas" FontSize="13" />
                </GroupBox>

                <GroupBox x:Name="DiagnosticsGroupBox" Grid.Row="3" Header="Diagnostics">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="LatencyText" Text="count=0 p50=0us p95=0us p99=0us" />
                        <TextBlock Grid.Row="1" x:Name="PixelText" Text="Pixel sampler: DXGI backend pending" Margin="0,8,0,0" />
                        <TextBlock Grid.Row="2" x:Name="DriverText" Text="HID transport: VHF backend pending" Margin="0,8,0,0" />
                    </Grid>
                </GroupBox>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
