<Window x:Class="MacroStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MacroHID Studio" Height="760" Width="1080"
        MinHeight="620" MinWidth="900"
        Background="#F5F5F7"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#F5F5F7" />
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#E5E5EA" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#1D1D1F" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#6E6E73" />
        <SolidColorBrush x:Key="AccentBrush" Color="#007AFF" />

        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="MinWidth" Value="92" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="Background" Value="#2F7CF6" />
            <Setter Property="BorderBrush" Value="#2F7CF6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1F6FE5" />
                    <Setter Property="BorderBrush" Value="#1F6FE5" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="#1D1D1F" />
            <Setter Property="Background" Value="#F2F2F7" />
            <Setter Property="BorderBrush" Value="#DADAE0" />
            <Setter Property="Height" Value="30" />
            <Setter Property="MinWidth" Value="78" />
            <Setter Property="Margin" Value="0,0,8,8" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{StaticResource PanelBrush}"
                                BorderBrush="{StaticResource PanelBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="12">
                            <DockPanel>
                                <ContentPresenter DockPanel.Dock="Top"
                                                  ContentSource="Header"
                                                  TextElement.FontWeight="SemiBold"
                                                  TextElement.FontSize="13"
                                                  TextElement.Foreground="{StaticResource SecondaryTextBrush}"
                                                  Margin="0,0,0,10" />
                                <ContentPresenter />
                            </DockPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#DADAE0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Padding" Value="8,4" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#DADAE0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#FFFFFF" BorderBrush="#E5E5EA" BorderThickness="0,0,0,1" Padding="16,12">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="MacroHID" Foreground="{StaticResource PrimaryTextBrush}" FontSize="19" FontWeight="SemiBold" Margin="0,0,18,0" />
                    <Button x:Name="OpenButton" Style="{StaticResource ToolbarButton}" Content="Open" Click="OpenMacro_Click" />
                    <Button x:Name="SaveButton" Style="{StaticResource ToolbarButton}" Content="Save" Click="SaveMacro_Click" />
                    <Button x:Name="ValidateButton" Style="{StaticResource ToolbarButton}" Content="Validate" Click="ValidateMacro_Click" />
                    <Button x:Name="ProbeButton" Style="{StaticResource ToolbarButton}" Content="Probe" Click="Probe_Click" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="LanguageLabelText" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" Text="Language" />
                    <ComboBox x:Name="LanguageComboBox"
                              Width="150"
                              Height="30"
                              Margin="0,0,18,0"
                              SelectionChanged="LanguageComboBox_SelectionChanged" />
                    <Border Background="#F2F2F7" BorderBrush="#E5E5EA" BorderThickness="1" CornerRadius="8" Padding="10,5">
                        <TextBlock x:Name="StatusText" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center" Text="Input backend ready" />
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Margin="16" Width="1048" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="420" MaxWidth="660" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="360" MinWidth="320" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" MaxWidth="660">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="MCRX" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBox x:Name="MacroEditor"
                         Grid.Row="1"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         FontFamily="Consolas"
                         FontSize="14"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap"
                         Padding="12"
                         BorderBrush="#C7CEDA"
                         Background="White" />
            </Grid>

            <Grid Grid.Column="2" Width="360">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <GroupBox x:Name="MacroGroupBox" Header="Macro">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="NameLabelText" Text="Name" Foreground="{StaticResource SecondaryTextBrush}" />
                            <TextBlock x:Name="MacroNameText" FontSize="20" FontWeight="SemiBold" Text="-" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock x:Name="ScheduledStepsLabelText" Text="Scheduled Steps" Foreground="{StaticResource SecondaryTextBrush}" />
                            <TextBlock x:Name="StepCountText" FontSize="20" FontWeight="SemiBold" Text="0" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox x:Name="PlaybackGroupBox" Grid.Row="1" Header="Playback">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="96" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.ColumnSpan="3">
                            <TextBlock x:Name="TriggerLabelText" Text="Trigger" Foreground="{StaticResource SecondaryTextBrush}" />
                            <DockPanel Margin="0,3,0,8">
                                <Button x:Name="CaptureTriggerButton" DockPanel.Dock="Right" Width="78" Content="Capture" Click="CaptureTrigger_Click" />
                                <TextBox x:Name="TriggerTextBox" Height="28" Margin="0,0,8,0" VerticalContentAlignment="Center" />
                            </DockPanel>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock x:Name="ModeLabelText" Text="Mode" Foreground="{StaticResource SecondaryTextBrush}" />
                            <ComboBox x:Name="PlaybackModeBox" Height="28" SelectionChanged="PlaybackModeBox_SelectionChanged">
                                <ComboBoxItem Tag="toggleLoop" Content="toggleLoop" />
                                <ComboBoxItem Tag="holdLoop" Content="holdLoop" />
                                <ComboBoxItem Tag="fixedCount" Content="fixedCount" IsSelected="True" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <TextBlock x:Name="PlaybackCountLabelText" Text="Count" Foreground="{StaticResource SecondaryTextBrush}" />
                            <TextBox x:Name="PlaybackCountTextBox" Height="28" Text="1" VerticalContentAlignment="Center" />
                        </StackPanel>

                        <WrapPanel Grid.Row="2" Grid.ColumnSpan="3" Margin="0,10,0,0">
                            <Button x:Name="StartListeningButton" MinWidth="104" Height="30" Margin="0,0,8,8" Content="Start Listening" Click="StartListening_Click" />
                            <Button x:Name="StopListeningButton" MinWidth="104" Height="30" Margin="0,0,8,8" Content="Stop Listening" Click="StopListening_Click" />
                            <Button x:Name="RunNowButton" MinWidth="78" Height="30" Margin="0,0,8,8" Content="Run Now" Click="RunNow_Click" />
                            <Button x:Name="StopPlaybackButton" MinWidth="64" Height="30" Margin="0,0,0,8" Content="Stop" Click="StopPlayback_Click" />
                        </WrapPanel>

                        <StackPanel Grid.Row="3" Grid.ColumnSpan="3">
                            <TextBlock x:Name="PlaybackStatusText" Text="Playback: Idle" />
                            <TextBlock x:Name="PlaybackResultText" Text="Last result: none" Margin="0,4,0,0" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox x:Name="ConverterGroupBox" Grid.Row="2" Header="Converter">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="ConverterText" Text="MacroConverter: checking" TextWrapping="Wrap" VerticalAlignment="Center" />
                        <Button x:Name="OpenConverterButton" Grid.Column="1" Content="Open Converter" Click="OpenConverter_Click" Margin="12,0,0,0" />
                    </Grid>
                </GroupBox>

                <GroupBox x:Name="SequenceGroupBox" Grid.Row="3" Header="Sequence">
                    <ListBox x:Name="StepList" FontFamily="Consolas" FontSize="13" />
                </GroupBox>

                <GroupBox x:Name="DiagnosticsGroupBox" Grid.Row="4" Header="Diagnostics">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="LatencyText" Text="count=0 p50=0us p95=0us p99=0us" />
                        <TextBlock Grid.Row="1" x:Name="PixelText" Text="Pixel sampler: DXGI backend pending" Margin="0,8,0,0" />
                        <TextBlock Grid.Row="2" x:Name="InputBackendText" Text="Input backend: SendInput ready" Margin="0,8,0,0" />
                    </Grid>
                </GroupBox>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
